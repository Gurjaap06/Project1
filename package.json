{
  "name": "project1_10",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:tw": "npx tailwindcss -i ./src/styles/app.css -o ./public/css/tailwind.css --watch",
    "dev:sass": "sass --no-source-map --watch src/scss:public/css",
    "dev:server": "nodemon server.js",
    "dev": "concurrently \"npm:dev:tw\" \"npm:dev:sass\" \"npm:dev:server\"",
    "build:tw": "npx tailwindcss -i ./src/styles/app.css -o ./public/css/tailwind.css --minify",
    "build:sass": "sass --no-source-map src/scss:public/css",
    "build": "npm run build:tw && npm run build:sass"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^9.2.0",
    "daisyui": "^4.12.24",
    "nodemon": "^3.1.10",
    "postcss": "^8.5.6",
    "sass": "^1.90.0",
    "tailwindcss": "^3.4.17"
  }
}
